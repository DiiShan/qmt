# QMT 数据权限二次审核意见（针对 commit 53a2583）

审核对象：commit `53a2583435a2fbd7bd50ecd7cc2ef38786c1eab1`

审核范围：**仅 XtData / 数据能力，不包含 XtTrader 或任何交易 API。**

参考：

- 迅投 XtData 主文档：https://dict.thinktrader.net/nativeApi/xtdata.html
- 迅投 xtquant 版本记录：https://dict.thinktrader.net/nativeApi/download_xtquant.html
- 迅投股票数据字典：https://dict.thinktrader.net/dictionary/stock.html
- 迅投期货数据字典：https://dict.thinktrader.net/dictionary/future.html
- 上一轮审核：`qmt权限-20260823审核意见.md`

## 1. 二次审核结论

本次更新已经解决上一轮大部分核心问题，报告可信度明显提高，可以继续作为当前账户的数据能力基线。

已经正确完成的改进包括：

- 审计范围已明确收窄到 XtData 数据能力，不再把 XtTrader 静态方法数量混入数据权限结论；
- 已记录 Python、xtquant、MiniQMT、revision/build 和 miniquote 版本；
- 已把 `ErrorID 300000` 与“官方 API 不存在”区分开，使用 `BACKEND_HANDLER_MISSING` 等原因码；
- 已把正订阅号与真实 callback 分开，休市日没有 callback 时不再标成实时 `PASS`；
- ETF、可转债、北交所股票、沪深300指数都补了真实最小行情样本；
- ETF IOPV、公告、涨跌停、港股通、转债特色、历史主力合约、历史期权等扩展数据已经显式纳入清单；
- `get_option_detail_data` 的统一异常已合理提升为 `CLIENT_SCHEMA_MISMATCH`，而不是误判为期权行情整体不可用；
- 报告明确区分命令受理（download/subscription accepted）和真实数据样本。

因此，当前结论可保留：

> 沪深 A 股 L1、ETF 通用行情、可转债通用行情、北交所通用行情、指数通用行情、八类财务数据已经有一笔式可用证据；A 股常规 Level 2 明确无权限；商品及金融衍生品的实时 callback 仍需在交易时段确认；多项扩展数据在当前 MiniQMT 后端不可用或未确认。

但本轮仍有以下需要 Codex 修正/补测的内容。

---

## 2. P0：实时 callback 探针仍可能产生假阳性

### 问题

当前 `subscribe_until_callback()` 使用：

```python
seq = method(code, period=period, count=1, callback=on_data)
```

迅投官方对 `subscribe_quote` 的说明是：`count/start_time/end_time` 描述订阅时请求的**历史部分**；如果只是实时订阅，通常使用 `count=0`。

因此 `count=1` 有潜在风险：订阅建立时返回的缓存/历史一笔可能进入 callback，从而被脚本误认为“收到实时行情”。

### 建议

实时权限验证改为：

```python
subscribe_quote(code, period="tick", count=0, callback=...)
```

并增加 freshness 校验：

- 记录 `subscribe_started_at`；
- callback 中提取数据 `time/stime`；
- 只有数据时间属于当前交易时段、且不早于订阅建立前的合理容差，才算 `CALLBACK_PASS`；
- 收到第一条合格 callback 后立即 unsubscribe。

这条应在下一轮补测前先修代码，否则交易日得到的 callback PASS 仍可能存在歧义。

---

## 3. P0：主探针与 P0/P1 探针的订阅判定仍不完全一致

`run_review_p0_p1()` 已经使用“真实 callback 才 PASS”的逻辑，这是正确的。

但完整主探针中仍有部分旧逻辑，例如 `subscribe_whole_quote` 只根据正订阅号 + unsubscribe 即标 `PASS`；部分千档订阅入口也仍采用“订阅号成功即 PASS”的 validator。

这会导致：

- 使用 `--review-p0-p1-only` 时结论严格；
- 以后运行完整探针时，可能重新出现“订阅受理 = 实时数据可用”的旧口径。

建议所有实时类 API 统一两层状态：

- `SUBSCRIBE_ACCEPTED`：seq > 0；
- `CALLBACK_PASS`：收到一条满足 freshness 条件的数据。

能力矩阵中的“实时数据可用”只使用第二层。

---

## 4. P0：商品衍生品仍漏了 INE 和 GFEX

当前 `discover_commodity_contracts()` 只覆盖：

- SHFE / `.SF`
- DCE / `.DF`
- CZCE / `.ZF`

迅投官方期货数据字典明确还包含：

- 上海国际能源交易中心 INE / `.INE`
- 广州期货交易所 GFEX / `.GF`

因此现在“商品期货/商品期权”仍不能称为国内商品衍生品完整覆盖。

下一轮各补一个当前有效代表合约即可：

| 市场 | 期货 | 期权（如有当前有效合约） |
|---|---|---|
| INE | 1d / 1m / tick / full tick / callback 各一笔 | 同样一笔式验证 |
| GFEX | 1d / 1m / tick / full tick / callback 各一笔 | 同样一笔式验证 |

无需批量测试。

---

## 5. P0：`ErrorID 300000` 不应全部强化为“版本不匹配”证据

当前报告已经比上一版准确，但“22 条 handler missing -> 包/后端不完全配套”的汇总仍需再细分。

原因是官方版本记录明确注明部分接口/数据需要额外版本或权限：

- `get_current_connect_sub_info`：投研版；
- `get_all_sub_info`：投研版；
- `get_order_rank`：投研版；
- `get_sector_info`：投研版；
- `get_transactioncount`：VIP；
- `snapshotindex`：VIP；
- `limitupperformance`：VIP；
- `historymaincontract`：VIP 数据；
- 订单流：订单流版权限。

所以对于这些项目，`300000 function not realize` 至少存在三种解释：

1. 券商 MiniQMT 后端版本确实较旧；
2. 当前是普通版而非投研版，对应 handler 未提供；
3. 当前账号没有该 VIP/增值数据服务，券商定制后端没有暴露相应 handler。

建议原因码改成更中性的：

`BACKEND_HANDLER_MISSING_OR_EDITION_UNAVAILABLE`

并增加一列 `official_prerequisite`（普通 / 投研 / VIP / 订单流 / L2）。

对于没有额外版权限要求、但官方早已支持且仍返回 300000 的接口，再作为更强的版本/后端不匹配证据。

另外，本机 MiniQMT build 日期为 2025-09-09，而官网当前 `xtquant_250807` 下载条目更新时间为 2025-12-19；Python 包新于券商客户端的可能性客观存在，但仍不应仅凭 premium API 的 300000 作唯一依据。

---

## 6. P1：遗漏“订单流数据”这一独立数据类别

迅投当前股票数据字典明确提供订单流 period：

- `orderflow1m`
- `orderflow5m`
- `orderflow15m`
- `orderflow30m`
- `orderflow1h`
- `orderflow1d`

其中历史数据只需要下载 `orderflow1m`，其他周期由 1m 合成；该数据需要“订单流版”权限。

当前报告未列此类别。

按本项目“一类一笔”原则，不需要六个周期全测：

1. `download_history_data("000001.SZ", period="orderflow1m")` 最小窗口；
2. `get_market_data_ex(... period="orderflow1m", count=1)`；
3. 取得一条 -> PASS；明确权限拒绝 -> NO_PERMISSION；handler 缺失 -> UNSUPPORTED；否则 EMPTY。

若 `orderflow1m` 成功，其他合成周期可只列“基础数据已具备，未逐周期验证”。

---

## 7. P1：遗漏历史 ST 专用 helper

当前报告列了 `specialtreatment`，但迅投股票数据字典还公开了独立接口：

- `download_his_st_data()`
- `get_his_st_data(stock_code)`

这是和 period `specialtreatment` 不完全相同的公开数据入口。

建议选一个官方示例或历史上确实有 ST 记录的 A 股，仅验证一条 ST 时间段即可。

注意：若选从未 ST 的股票，返回 `{}` 不能证明接口不可用，因此代表代码应选择已知有 ST 历史的股票。

---

## 8. P1：`snapshotindex` 当前测试流程不足以判定

迅投官方数据字典对 `snapshotindex` 明确注明：

- VIP 数据；
- 获取时需要先通过 `subscribe_quote` 订阅。

当前 P0/P1 脚本对它使用普通 `get_market_data_ex` + 可选 `download_history_data`，没有先建立 `snapshotindex` 订阅。

因此当前 `EMPTY` 不能作为高质量能力结论。

建议在 A 股交易时段：

```python
seq = xtdata.subscribe_quote("000001.SZ", period="snapshotindex", count=0, callback=...)
```

收到一条 -> PASS；明确 VIP/权限拒绝 -> NO_PERMISSION；无回调 -> EMPTY/UNKNOWN；然后立即取消。

---

## 9. P1：下载接口的“异步/非阻塞”描述需要修正文案

当前代码和报告中把部分接口描述为“异步”或“非阻塞兼容入口”。

但迅投当前 XtData 主文档对以下两个接口均写明“同步执行，补充数据完成后返回”：

- `download_history_data2(...)`
- `download_financial_data2(...)`

它们带 callback 是为了报告进度，不等于函数本身一定异步返回。

因此建议将文案改成：

- `download_history_data2`：批量下载接口（带进度 callback）；
- `download_financial_data2`：带时间范围和进度 callback 的财务下载接口；
- 如果在本券商客户端实测行为与官网不同，可注明“本机实测返回行为”，不要把它泛化为官方语义。

这不会改变已取得的财务数据 PASS，只是修正文档准确性。

---

## 10. P1：期权市场覆盖还可补两个代表域

当前已测试上证证券期权 `SHO`，但没有单独验证：

- 深证证券期权 `SZO`；
- 中金所股指期权（市场 `IF`，ProductType 可为期权）。

由于不同市场的代码格式、后端 handler 和 schema 可能不同，如果目标是“当前账户所有国内衍生品数据权限”，建议各取一个当前有效合约，仅做：

- instrument detail；
- 1d；
- 1m；
- tick；
- full tick；
- 交易时段一条 callback。

仍然是一类一笔，不需要全合约扫描。

---

## 11. P2：如目标是“全部可提供的数据资产”，还应明确是否纳入以下域

这些不是当前报告的致命缺口，但若标题继续使用“QMT 数据权限审计”，建议至少显式列 OUT_OF_SCOPE / NOT_TESTED：

- 非 ETF 基金 / LOF；
- 普通债券、国债/企业债等固定收益证券；
- 回购品种；
- 板块指数 `BKZS`；
- 港股通用 L1、港股 L2；
- 美股；
- 外盘。

如果项目实际只关心 A 股、ETF、转债、期权和期货，则可以直接声明这些为 OUT_OF_SCOPE，不需要测试。

---

## 12. 代码可维护性小问题

`qmt_api_probe_minimal.py` 的 `--miniqmt-dir` 默认值写死了本机安装路径。

建议改为：

1. 默认空字符串；
2. 从环境变量读取；或
3. 运行时显式传 `--miniqmt-dir`。

这样仓库更可移植，也避免把机器目录结构长期固化在公开代码中。

---

## 13. 下一轮 Codex 推荐顺序

仍坚持“一类数据只取一笔”。

### 先修探针，不需要交易时段

1. 实时 callback 的 `count=1 -> count=0`，增加时间 freshness 校验；
2. 统一所有 subscription 的 `SUBSCRIBE_ACCEPTED` / `CALLBACK_PASS` 两层口径；
3. `300000` 增加 edition/VIP prerequisite 分类；
4. 修正 `download_history_data2` / `download_financial_data2` 的同步文案；
5. 增加 INE、GFEX；
6. 增加 orderflow、ST helper、SZO、股指期权清单。

### 交易时段只做最小复测

- A 股：1 条 tick callback；
- ETF：1 条 callback；
- 可转债：1 条 callback；
- 北交所：1 条 callback；
- CFFEX 期货：1 条 callback；
- SHFE/DCE/CZCE/INE/GFEX：每市场各 1 个期货代表合约 callback；
- 对有期权的代表市场只需各 1 个期权 callback；
- `snapshotindex`：按官方流程订阅 1 条；
- orderflow：历史 `orderflow1m` 取 1 条即可，不要求实时。

### 完成标准

对每类数据只回答一个问题：

> **当前 MiniQMT + 当前账号能否通过 Python API 取得至少一个合法样本？**

不要为了“覆盖率”扩大下载量，也不要把订阅号、download 命令返回、静态方法存在本身计作数据 PASS。
