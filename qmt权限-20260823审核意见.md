# QMT 权限审计报告审核意见（针对 2026-08-23 版本）

审核对象：`qmt权限-20260823版本.md`

审核日期：2026-08-23

参考：

- 迅投 XtData 主文档：https://dict.thinktrader.net/nativeApi/xtdata.html
- 迅投 XtTrader 主文档：https://dict.thinktrader.net/nativeApi/xttrader.html
- 迅投 xtquant 版本记录：https://dict.thinktrader.net/nativeApi/download_xtquant.html

## 1. 总体结论

原报告的主要实测结论可以保留：

- 沪深 A 股 Level 1 的历史 K 线、tick、快照及基本订阅链路已取得有效证据；
- A 股常规 Level 2 和千档盘口已取得明确 `NO_PERMISSION` 证据；
- 证券期权、股指期货存在部分可用能力；
- 商品期货、商品期权目前只能判定为“行情权限未知”，不能根据休市时的 EMPTY 下结论；
- 八类财务数据已取得有效样本；
- XtTrader 目前只是静态接口审计，不能据此推断资金账号交易权限。

但是，当前版本不宜再表述为“XtData 官网主接口 43 项 + 版本记录扩展 7 项已经覆盖全部能力”。官方 `xtquant_250807` 版本记录中还存在多批未纳入此次实机审计的 XtData 能力。因此，本报告应定位为：

> **核心行情/基础数据权限审计 + 部分扩展接口审计，而不是完整 XtData 全能力审计。**

此外，当前最需要确认的不是新增更多批量测试，而是 **Python `xtquant 250807.1.2` 与实际登录的券商 MiniQMT 客户端/行情后端是否版本匹配**。

---

## 2. P0：必须先确认的问题

### 2.1 Python 包版本与 MiniQMT 后端版本可能不匹配

原报告只记录了：

- Python 3.13.5
- `xtquant 250807.1.2`

但没有记录实际 MiniQMT 客户端的：

- 客户端完整版本号 / build；
- 券商定制版本号；
- `userdata_mini` 对应客户端；
- 当前行情连接模式；
- 是否投研版 / 普通券商版。

当前多个现象共同指向“Python 包暴露了接口，但 MiniQMT 后端 handler 较旧或能力未实现”的可能性：

- `get_period_list` -> ErrorID 300000 `function not realize`
- `get_trading_calendar` -> ErrorID 300000
- `get_etf_info` -> ErrorID 300000
- `get_full_kline` -> ErrorID 300000
- `get_l2thousand_queue` -> ErrorID 300000
- `get_trading_period` -> ErrorID 300000
- `get_option_detail_data` -> 客户端返回字段与 Python wrapper 预期不一致而触发 TypeError

尤其官方版本记录表明，上述多项能力在 250807 之前已经加入公版 xtquant。如果本机 Python 包是 250807，而券商 MiniQMT 内部后端较旧，就会出现“方法存在但服务端 function not realize / schema 不匹配”的情况。

### 建议补充记录

新增“环境兼容性”表：

| 项目 | 实测值 |
|---|---|
| Python version | 3.13.5 |
| xtquant package | 250807.1.2 |
| MiniQMT 客户端版本/build | **待确认** |
| 券商 | 兴业证券 |
| MiniQMT / QMT 模式 | **待确认** |
| 普通版/投研版 | **待确认** |
| 行情连接服务器/模式 | 只记录类型，不记录敏感地址 |

在此确认之前，建议把 ErrorID 300000 的状态解释改成：

> `CLIENT_UNSUPPORTED / BACKEND_HANDLER_MISSING`（当前 MiniQMT 后端未实现）

而不是容易被理解为“XtQuant 官方 API 本身不支持”。

---

### 2.2 “43 + 7 已完整覆盖”需要修正

官方版本记录中，当前 `xtquant_250807` 及其之前版本至少还列出了下列未在原报告 43+7 表中完整纳入的 XtData 能力。

#### 250807 / 250516

- `get_current_connect_sub_info`（投研版）
- `get_all_sub_info`（投研版）
- `get_order_rank`（投研版）
- `get_transactioncount`（大单统计，VIP）
- `get_sector_info`（带分类信息板块，投研版）
- `get_tabular_data`（版本记录明确提及）

#### 241014 / 240920

- `get_formula_result`
- `get_trading_contract_list`
- `get_trading_period`
- `get_all_trading_periods`
- `get_all_kline_trading_periods`
- `subscribe_quote2`

原报告只通过兼容名测试了部分 trading-period 能力，没有覆盖上述完整系列。

#### 240812

- `get_his_option_list`
- `get_his_option_list_batch`
- `get_option_undl_data`
- `get_option_list`

#### 240329 及之前

- `watch_quote_server_status`
- `watch_xtquant_status`
- `get_broker_queue_data`
- `subscribe_quote(period='brokerqueue')`

因此建议把原报告标题中的“覆盖口径”改成：

> XtData 主文档 43 项已审计；另对部分版本记录扩展能力进行了审计。版本记录扩展接口尚未完全覆盖，详见审核意见的待确认清单。

不建议给“官网全部能力”一个固定 43+7 的总数，除非以后通过当前安装包 `dir(xtdata)` + 官方版本记录逐项建立完整 manifest。

---

## 3. P1：数据种类仍有明显遗漏

本项目目标是“每类数据取一笔，只确认是否可行”，因此下面项目都只需要一个最小样本，不需要批量下载。

### 3.1 ETF 通用行情没有完整验证

当前报告只确认：

- ETF `get_instrument_detail` 可用；
- `get_etf_info` 专用申赎接口不支持；
- 日线/分钟写成“通过通用行情可读”，但 tick、快照、订阅没有单列证据。

建议至少对 `510300.SH` 补充：

- `1d`: count=1
- `1m`: count=1
- `tick`: count=1
- `get_full_tick`: 1 个对象
- `subscribe_quote`: 实际收到 1 次 callback 后立即取消（交易时段）

另外官方版本记录还有 ETF IOPV 数据：

- `etfiopv1m`
- `etfiopv1d`

这两个应至少列为 `NOT_TESTED`，如接口/数据源允许则各取 1 笔。

### 3.2 可转债只测了专用元数据，遗漏通用行情

`get_cb_info` 返回 EMPTY 并不能代表“可转债行情未知”。

迅投历史版本明确补充过转债交易场景数据支持。应对一个当前有效可转债补测：

- 1d
- 1m
- tick
- full tick
- subscribe callback

每类只需要 1 笔。

同时补列：

- `delistchangebond`
- `replacechangebond`

作为转债特色数据能力，至少标记 `NOT_TESTED`，不要只依赖 `get_period_list` 是否可用。

### 3.3 北交所未覆盖

官方版本记录已经明确支持北交所相关交易/板块能力。当前报告“沪深 A 股 L1”不能代表全部 A 股市场。

建议增加一个当前有效北交所股票，最小测试：

- instrument detail
- 1d
- 1m
- tick/full tick
- subscribe callback

如果用户实际不需要北交所，也应明确标记 `OUT_OF_SCOPE`，而不是隐含在“A股”结论内。

### 3.4 指数域只有指数权重，没有指数行情

报告声称覆盖“指数”资产域，但当前实测主要是 `get_index_weight(000300.SH)`，且结果 EMPTY。

建议对 `000300.SH` 补一个通用行情样本：

- 1d 或 1m 任一有效 bar；
- full tick 任一有效 snapshot。

这样才能确认“指数行情”本身是否可行；指数行情与“指数成分权重下载”是两个不同能力。

### 3.5 官方版本记录中的特色 period 未完整列入

除当前报告已经列出的 research 项外，还应纳入清单（至少 `NOT_TESTED`）：

- `announcement` 新闻公告
- `limitupperformance` 涨跌停/连板数据
- `hktdetails` 港股通持股明细
- `hktstatistics` 港股通统计
- `etfiopv1m`
- `etfiopv1d`
- `stoppricedata`
- `delistchangebond`
- `replacechangebond`
- `historymaincontract`
- `optionhistorycontract`
- `snapshotindex`
- `brokerqueue`

`get_period_list` 在当前 MiniQMT 后端不可用，不应成为“不再测试已知特色 period”的阻塞条件。对官方已经有明确 period 名称和适用标的的项目，可以直接做一笔 schema-appropriate 测试；无法构造合法样本的再标记 `NOT_TESTED`。

### 3.6 港股 / 美股 / 外盘服务未纳入范围声明

官方版本记录提到：

- 港股 L2 / broker queue 相关能力；
- 美股行情；
- 外盘行情（需购买相应服务）。

本次报告没有测试这些市场。如果用户目标是“当前账户全部行情权限”，应在总表中明确列：

- 港股：NOT_TESTED / 未购买 / 不在范围（按真实情况）
- 美股：NOT_TESTED / 未购买 / 不在范围
- 外盘：NOT_TESTED / 未购买 / 不在范围

否则“数据资产域覆盖”容易被理解成已经覆盖所有 MiniQMT 可提供市场。

---

## 4. P1：实时订阅 PASS 的证据等级需要再拆分

当前报告已经正确写明：

> 订阅号大于零只证明客户端接受订阅，不表示交易时段一定能收到回调。

但主表仍把商品期货/商品期权的 `subscribe_quote` 标成 PASS，容易在阅读摘要时被误读为“实时行情可用”。

建议把实时订阅拆成两层证据：

- `SUBSCRIBE_ACCEPTED`：返回 seq > 0；
- `CALLBACK_PASS`：交易时段实际收到 1 条合法数据。

最终“实时行情可用”应以 `CALLBACK_PASS` 为准。

这项复测只需要：

1. 交易时段订阅 1 个标的；
2. 收到第一条 callback；
3. 记录字段和时间戳；
4. 立即 unsubscribe。

商品期货和商品期权当前最需要做这一步。

---

## 5. P1：`get_option_detail_data` ERROR 应优先按 schema/version mismatch 排查

当前证券期权和三类商品期权都在同一 wrapper 位置发生 TypeError，且 `get_instrument_detail` 又能正常返回合约资料。

这不像单个合约数据异常，更像：

- Python wrapper 版本较新；
- MiniQMT 返回字段较旧；
- 新版 wrapper 访问了服务端没有返回的字段。

官方版本记录中，`get_option_detail_data` 曾多次增加/调整字段和实现，例如：

- `OptUndlCodeFull`
- `OptEstimatedMargin`
- 商品期权支持及方向判断修复

建议下一轮记录：

- 完整 exception 类型和 message（去敏）；
- wrapper 报错的具体缺失字段名；
- MiniQMT build；
- `get_instrument_detail(..., True)` 对该期权返回的字段集合。

如果确认是字段协议不一致，应将结论改为 `CLIENT_SCHEMA_MISMATCH`，而不是泛化为“期权专用详情接口不可用”。

---

## 6. P1：`get_full_kline` 需要区分“客户端 handler 不支持”和“未开启 K 线全推”

官方版本记录说明 `get_full_kline()` 用于批量获取当日 K 线，并注明需要开启 K 线全推。

当前测试得到 ErrorID 300000，应补确认：

- 当前券商 MiniQMT 是否有 K 线全推功能；
- 是否已经开启；
- 当前 MiniQMT build 是否实现该 handler。

在这三项未确认前，建议结论写：

> 当前 MiniQMT 环境不可用，原因可能是后端版本或 K 线全推能力/配置，不属于行情权限的直接证据。

---

## 7. P2：PASS 数量不能等同于“可用数据权限数量”

报告顶部的 72 个 PASS 包含：

- 下载请求成功；
- unsubscribe 成功；
- 静态/元数据接口；
- 订阅号被接受；
- 实际数据返回。

因此“72 PASS”只是 **163 条 API 检查记录中的成功调用数**，不是“72 类数据权限可用”。

建议摘要同时增加两个指标：

1. API-call status 统计（保留现有 72/52/...）；
2. Data-capability status 统计，只统计真实数据域，例如 A股1d、ETF tick、财务 Balance、L2 order 等。

对 `download_*` 类接口建议记录为 `COMMAND_ACCEPTED` 或在解释中明确：下载命令成功本身不证明服务器最终有该数据。

---

## 8. P2：XtTrader 不宜放在“权限已审计”的同一结论层级

当前 XtTrader 只做静态 introspection，没有：

- connect；
- subscribe account；
- query account status；
- query asset/position/order/trade。

所以当前只能说明“Python 包暴露了这些方法”，不能说明“该账户拥有这些权限”。原报告已有这条免责声明，应进一步在标题中把 XtTrader 标记为：

> **API surface audit only / 权限未验证**

如果以后需要验证 XtTrader，但仍保持只读安全，可以单独建立一个只读测试阶段，只执行：

- `connect`
- `subscribe`
- `query_account_status`
- `query_stock_asset`
- `query_stock_positions`
- `query_stock_orders`
- `query_stock_trades`

不执行任何 order / cancel / transfer。测试输出必须去除资金账号和金额后再提交公开仓库。

---

## 9. 建议新增的最小复测清单

仍遵循“一类只取一笔”。优先顺序如下。

### P0：环境兼容性

- [ ] 记录 MiniQMT 客户端完整 build/version
- [ ] 确认普通券商版 / 投研版
- [ ] 确认 `xtquant 250807.1.2` 是否与该 MiniQMT build 配套
- [ ] 对 ErrorID 300000 统一标记 backend handler missing，而非官方 API unsupported

### P1：250807/250516 扩展能力

- [ ] `get_current_connect_sub_info`
- [ ] `get_all_sub_info`
- [ ] `get_order_rank`
- [ ] `get_transactioncount`
- [ ] `get_sector_info`
- [ ] `get_tabular_data`（先做静态存在性/签名确认，再决定最小安全调用）

### P1：其他官方新增 API

- [ ] `get_formula_result`（无投研公式可保持 SKIP）
- [ ] `get_trading_contract_list`
- [ ] `get_all_trading_periods`
- [ ] `get_all_kline_trading_periods`
- [ ] `subscribe_quote2`
- [ ] `get_his_option_list`
- [ ] `get_his_option_list_batch`
- [ ] `get_option_undl_data`
- [ ] `get_option_list`
- [ ] `watch_quote_server_status`
- [ ] `watch_xtquant_status`
- [ ] `get_broker_queue_data` / `brokerqueue`

### P1：遗漏资产/数据域

- [ ] ETF tick/full tick/callback
- [ ] ETF IOPV 1m/1d
- [ ] 可转债 1d/1m/tick/full tick/callback
- [ ] 北交所股票 1d/1m/tick/full tick/callback
- [ ] 指数 1d + full tick
- [ ] `announcement`
- [ ] `limitupperformance`
- [ ] `stoppricedata`
- [ ] `snapshotindex`
- [ ] `delistchangebond`
- [ ] `replacechangebond`
- [ ] `historymaincontract`
- [ ] `optionhistorycontract`
- [ ] 港股/美股/外盘明确标记 NOT_TESTED / OUT_OF_SCOPE / NO_SERVICE

### P1：交易时段确认

- [ ] 商品期货：收到 1 条 callback 即停止
- [ ] 商品期权：收到 1 条 callback 即停止
- [ ] 股指期货：收到 1 条 callback 或 fresh full tick 即停止
- [ ] 证券期权历史 tick 如有需要可再补 1 条；否则保持历史 tick EMPTY，不影响已证明的 K 线/快照能力

---

## 10. 建议修改原报告的摘要措辞

建议将：

> XtData 官网主接口区：43 个函数入口，全部纳入检查。XtData 官网版本记录中的扩展能力：7 项，全部纳入检查。

改为：

> XtData 主文档列出的 43 个函数入口已纳入检查；另测试了部分版本记录扩展能力。官方版本记录仍存在尚未纳入本轮实机测试的新增接口、特色 period 和市场域，因此本报告是核心能力审计，不宣称覆盖当前 xtquant 的全部 XtData surface。

并将：

> `UNSUPPORTED`：当前客户端明确不支持、处理函数不存在或接口名不存在

进一步拆解解释为：

- Python 方法不存在：`PY_API_ABSENT`
- MiniQMT handler `function not realize`：`CLIENT_UNSUPPORTED / BACKEND_HANDLER_MISSING`
- period 无效：`INVALID_PERIOD / CLIENT_UNSUPPORTED`
- 明确权限错误：`NO_PERMISSION`

这样后续升级 MiniQMT 或更换匹配的 xtquant 包时，可以清楚知道哪些项目值得重测。

---

## 11. 审核后的当前可信结论

在上述遗漏尚未补测前，建议把 2026-08-23 权限状态概括为：

1. **沪深 A 股 L1：已确认可用。**
2. **A 股 Level 2：已确认无权限。**
3. **财务八表：已确认可用。**
4. **证券期权：K 线/快照/订阅受理可用；专用详情存在客户端 schema/版本疑点。**
5. **股指期货：历史 K 线可用；实时 fresh tick/callback 尚需交易时段确认。**
6. **商品期货/商品期权：合约资料和订阅受理可用，但行情数据权限仍未知，必须交易时段取得 1 条真实 callback 才能确认。**
7. **ETF：通用行情已有部分证据，但 tick/快照/callback、IOPV 尚未形成完整一笔式能力证据。**
8. **可转债：专用元数据 EMPTY，通用行情尚未充分审计。**
9. **北交所、指数通用行情、港美股/外盘及多类特色 period 尚未完整审计。**
10. **XtTrader：仅完成 API surface 静态审计，资金账号权限未验证。**
11. **多个 ErrorID 300000 / option schema error 强烈建议先核对 MiniQMT build 与 xtquant 250807.1.2 的配套关系。**

以上项目全部可以继续遵守“每类只取一笔”的原则完成，不需要批量下载或全市场扫描。
