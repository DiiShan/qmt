# QMT 本地数据库构建计划

> 版本：2026-08-23
>
> 目标：基于 MiniQMT / XtData 构建一个可持续增补、可复用、容量受控的本地日频量化研究数据底座，直接服务于后续因子研究、策略、回测、绩效与风险分析。
>
> 当前范围只包含 **数据与研究**，不包含 XtTrader、下单、撤单、资金账户或任何交易 API。

---

## 1. 唯一标准架构（Canonical Architecture）

本项目统一采用以下架构，不再并行设计第二套数据框架：

```text
MiniQMT / XtData
        ↓
采集 / 增量更新
        ↓
Raw Parquet
        ↓
清洗 / 标准化 / Point-in-Time
        ↓
Processed Parquet
        ↓
DuckDB（Catalog / View / SQL Query Layer）
        ↓
Research Dataset
        ↓
Factor / Strategy / Backtest / Risk
```

### 关键解释

1. **Raw Parquet 是不可变原始层**
   - 尽量忠实保存 QMT 返回字段。
   - 不直接做前复权覆盖。
   - 不因后续清洗逻辑变化而修改历史 Raw 文件。

2. **Processed Parquet 是标准研究层**
   - 统一字段名、类型、主键和日期。
   - 做去重、质量检查、证券状态、复权相关标准化。
   - Point-in-Time 逻辑在这里形成可靠的研究输入。

3. **DuckDB 默认不复制完整 Parquet 数据**
   - 主要创建 View、Catalog、Metadata 和必要的小型物化表。
   - 通过 `read_parquet()` / views 查询 Raw / Processed。
   - 避免 Raw + Processed + DuckDB 再形成第三份完整行情副本。

4. **Factor / Strategy / Backtest / Risk 是 DuckDB 之上的研究层**
   - 因子可保存为版本化 Parquet。
   - 策略信号、回测结果、风险结果分别保存。
   - 它们不改变底层行情和财务数据库。

5. **期货主链、基差等属于 Derived / Research Dataset**
   - 底层仍保存真实期货合约。
   - 主力映射和连续序列由真实合约数据派生。
   - 不是另外一套数据库架构。

---

## 2. 建设目标

v1 优先建立一个可靠的 **日频研究核心数据库**，而不是下载当前权限下所有可获得数据。

第一阶段必须支持：

- A 股历史日线研究；
- 历史股票池；
- 复权与真实收益率；
- 财务 Point-in-Time；
- 指数 benchmark；
- 股指期货主力合约映射；
- 股指期货升贴水 / 基差研究；
- 后续因子、策略、回测和风险模块直接复用同一数据底座；
- 新数据类型可以以新增 dataset 的方式接入，而不重构现有层级。

---

## 3. v1 数据范围

### 3.1 A 股

市场：

```text
SH + SZ + BJ
```

周期：

```text
1d only
```

不建设：

- 全市场分钟历史；
- Tick 历史；
- Level 2；
- 实时全推仓库。

### 3.2 历史时间范围

默认：

```text
2011-01-01 → 当前
```

最低可接受回退：

```text
2014-01-01 → 当前
```

原因：仅保留滚动 10 年会漏掉 2014–2015 牛市、2015 股灾和 2016 年初熔断等重要 A 股压力区间，而日线容量并不是主要约束。

如 QMT 某类数据历史覆盖不足，应保留实际最早可用日期，并在 metadata 中记录，不伪造完整历史。

### 3.3 A 股日线字段

Raw 层尽量保留 QMT 原始返回字段。

Processed 核心字段至少包括：

```text
trade_date
stock_code
open
high
low
close
pre_close
volume
amount
suspend_flag
```

原始行情不直接覆盖为前复权行情。

### 3.4 证券主表 / 历史 Universe

核心表：

```text
security_master
```

字段至少包括：

```text
stock_code
stock_name
exchange
security_type
board
list_date
delist_date
```

必须覆盖历史退市证券，不能只使用当前仍上市股票，否则会产生 Survivorship Bias。

### 3.5 交易日历

```text
trade_calendar
```

当前环境优先使用已经确认可用的 `get_trading_dates` 构建，不等待 `get_trading_calendar`。

### 3.6 除权除息与复权

至少建立：

```text
corporate_action
adjust_factor
```

要求可以从同一 Raw 数据动态产生：

- 原始价格；
- 前复权；
- 后复权；
- 策略收益率需要的复权序列。

### 3.7 财务八表

保存当前已经确认可用的：

```text
Balance
Income
CashFlow
Capital
HolderNum
Top10Holder
Top10FlowHolder
PershareIndex
```

必须重点保留：

```text
stock_code
report_period
announce_date
```

回测中只能在 `announce_date` 之后使用该记录。

### 3.8 主要指数

v1 至少包括：

```text
000001.SH   上证指数
000016.SH   上证50
000300.SH   沪深300
000905.SH   中证500
000852.SH   中证1000
```

用于 benchmark、市场状态与股指期货基差。

### 3.9 股指期货

v1 优先 CFFEX：

```text
IF / IH / IC / IM
```

底层必须保存 **真实历史合约**，例如：

```text
IF2609.IF
IF2612.IF
...
```

不要把 `IF00` 一类主连作为底层唯一数据。

期货日线核心字段：

```text
trade_date
contract_code
product
open
high
low
close
settlement
volume
open_interest
```

合约主表：

```text
future_contract_master
```

至少包括：

```text
contract_code
product
exchange
list_date
expire_date
multiplier
```

### 3.10 期货主链与基差

派生数据：

```text
future_main_mapping
future_main_daily
future_basis_daily
```

第一版主力规则：

```text
主规则：最大 open_interest
fallback：最大 volume
```

必须保留 `selection_method`，后续可以新增其他主力定义而不覆盖旧版本。

基差至少保留：

```text
trade_date
product
contract_code
spot_code
future_close
future_settlement
spot_close
basis_close
basis_settlement
basis_pct
days_to_expiry
annualized_basis
is_main_contract
```

股指期货映射：

```text
IH → 000016.SH
IF → 000300.SH
IC → 000905.SH
IM → 000852.SH
```

---

## 4. 暂不进入 v1 的数据

为了控制容量和系统复杂度，第一阶段明确不建设：

- 期权；
- Level 2；
- 全 A 分钟历史；
- 全 A Tick 历史；
- 港股、美股、外盘；
- 商品现货；
- ETF IOPV / VIP / 订单流等特色数据；
- 商品期货全市场历史（权限确认后按研究需求增补）；
- 交易相关数据。

这些未来应以 **新增 dataset** 的形式接入，不改变 Raw → Processed → DuckDB → Research 的主架构。

---

## 5. 空间预算与硬约束

总原则：**100GB 是总上限，不是使用目标。**

本项目必须为未来分钟数据、ETF、可转债、商品期货等扩展保留明显空间。

### 5.1 v1 持久化目标

建议长期持久化：

| 数据 | 目标预算 |
|---|---:|
| A 股 Raw 日线 | 3–5 GB |
| A 股 Processed 日线 | 2–4 GB |
| 财务 Raw + Processed | 3–6 GB |
| 证券主表 / 状态 / 交易日历 / 复权 | 1–2 GB |
| 指数 + CFFEX 真实合约 + 主链 / 基差 | < 1 GB |
| DuckDB Catalog / View / Metadata | < 2 GB |
| 日志 / checkpoint / quality report | < 1 GB |
| **v1 持久化合理范围** | **10–20 GB** |

这是工程预算，不是对 QMT 实际压缩率的承诺；Codex 必须在真实数据落盘后报告实际大小。

### 5.2 容量控制阈值

基础数据库使用以下阈值：

```text
TARGET        <= 25 GB
WARNING       >= 30 GB
HARD LIMIT    >= 40 GB
```

含义：

- 25GB：希望 v1 长期稳定维持在该范围内；
- 30GB：必须输出空间异常报告，分析最大目录、重复副本和压缩情况；
- 40GB：基础库停止新增大规模 dataset，必须先处理容量问题或显式调整配置。

这样至少为后续数据扩展保留约 60GB。

### 5.3 额外需要监控 MiniQMT Cache

MiniQMT / XtData 下载历史时可能产生自身本地缓存，因此空间监控必须区分：

```text
project_data_size
qmt_cache_size
temp_size
free_disk_space
```

项目数据库本身不得因为 QMT cache 已经存在，就无条件复制多份数据。

### 5.4 禁止不必要复制

禁止默认：

```text
Raw Parquet
+ Processed Parquet
+ DuckDB Full Copy
+ CSV Full Copy
```

标准方案是：

```text
Raw Parquet
+ Processed Parquet
+ DuckDB Views / Catalog
```

CSV 仅用于极小调试样本，不作为正式存储。

### 5.5 分区和压缩

默认使用：

```text
Parquet + ZSTD
```

建议按 dataset 特征分区，不创建大量极小文件。

A 股日线优先考虑：

```text
processed/market/daily/year=YYYY/*.parquet
```

或经过基准测试后按年度 + 合理 bucket 分区。

不要默认“一股票一个小文件 × 每年一个文件”，避免产生过多文件。

---

## 6. 推荐目录

```text
data/
├── raw/
│   ├── market/
│   │   ├── stock_daily/
│   │   ├── index_daily/
│   │   └── futures_daily/
│   ├── financial/
│   ├── reference/
│   └── corporate_action/
│
├── processed/
│   ├── market/
│   ├── financial/
│   ├── reference/
│   ├── universe/
│   └── status/
│
├── derived/
│   ├── adjusted_price/
│   ├── futures_main/
│   └── futures_basis/
│
├── factors/
│
├── strategy/
│
├── backtest/
│
├── risk/
│
├── database/
│   └── qmt.duckdb
│
└── logs/
```

所有大数据目录必须加入 `.gitignore`，GitHub 只保存代码、schema、配置、报告和小型样本。

---

## 7. Codex 执行计划

# Phase 0 — Preflight

在正式全量下载前，只做最小样本闭环。

必须验证：

1. `download_history_contracts()` 是否可执行；
2. 能否发现至少一个历史退市 A 股；
3. 对该退市 A 股调用 `get_instrument_detail()`；
4. 能否取得该退市 A 股至少一根历史 `1d`；
5. 能否发现 CFFEX 历史/过期期货实际合约；
6. 能否取得一个过期期货合约至少一根 `1d`；
7. 期货日线是否包含 `settlementPrice`、`openInterest`、`volume`；
8. 当前 SHFE / DCE / CZCE 各测试一个 `1d`，仅作为未来扩展能力记录；
9. 如可以发现 INE / GFEX 当前合约，各测试一个 `1d`。

每类只需要一个有效样本。

### Phase 0 Gate

以下项目必须 PASS 才能进入全量核心库构建：

```text
当前 A 股日线
历史退市 A 股发现
历史退市 A 股日线
CFFEX 历史合约发现
CFFEX 过期期货日线
```

商品期货不是 v1 blocker。

---

# Phase 1 — 项目骨架与配置

建立：

```text
config/data_config.yaml
src/
scripts/
tests/
data/（git ignored）
```

配置文件至少包含：

```yaml
history_start: 2011-01-01
stock_period: 1d
compression: zstd
storage_target_gb: 25
storage_warning_gb: 30
storage_hard_limit_gb: 40
```

实现 `storage_guard.py`。

每次批量下载前后记录：

```text
project_data_size
qmt_cache_size（若可定位）
temp_size
free_disk_space
```

---

# Phase 2 — Security Master / Historical Universe

构建当前 + 历史证券全集。

输出：

```text
processed/reference/security_master.parquet
```

要求支持：

```text
list_date
delist_date
is_listed(trade_date)
```

验收：历史日期的股票数量必须随时间变化，不能等于当前股票总数。

---

# Phase 3 — A 股历史日线

范围：

```text
SH + SZ + BJ
2011-01-01 → 当前
1d
```

如果 QMT 不能可靠覆盖 2011，则记录实际最早日期；只有必要时回退统一基准到 2014-01-01。

下载流程：

```text
Download
  ↓
Read QMT cache
  ↓
Validate minimal schema
  ↓
Write Raw Parquet
  ↓
Checkpoint
  ↓
Transform
  ↓
Write Processed Parquet
```

必须可断点续传、可重复执行且不产生重复数据。

不要一次请求整个市场全部历史。

Batch 大小通过本机 benchmark 决定，默认从 100–300 stocks / batch 小规模测试开始。

---

# Phase 4 — 财务 / 复权 / Reference

建立：

```text
financial_*
corporate_action
adjust_factor
trade_calendar
stock_status
```

财务表必须通过 `announce_date` 建立 Point-in-Time 语义。

ST 历史专用 helper 当前不可用时，不得伪造 ST 历史；先标记数据缺口，并寻找现有基础字段能否可靠重建。无法重建则在 metadata 中明确 `UNKNOWN`。

---

# Phase 5 — 指数 + CFFEX 实际合约

下载主要指数及：

```text
IF / IH / IC / IM
```

所有可发现的真实历史合约日线。

不依赖当前不可用的 `historymaincontract` 作为唯一来源。

---

# Phase 6 — Derived Data

建立版本化派生数据：

```text
future_main_mapping
future_main_daily
future_basis_daily
adjusted_price
historical_universe
```

派生层允许删除并从 Processed 重建。

---

# Phase 7 — DuckDB Query Layer

创建：

```text
data/database/qmt.duckdb
```

原则：

- 大表默认 View over Parquet；
- 小型 metadata / schema / quality summary 可以物化；
- 不默认复制一份完整行情进入 DuckDB。

必须支持至少：

```sql
-- 单证券时间序列
SELECT * FROM daily_bar
WHERE stock_code = '600519.SH'
ORDER BY trade_date;

-- 单日全市场横截面
SELECT * FROM daily_bar
WHERE trade_date = DATE '2020-06-30';
```

---

# Phase 8 — Data Quality

行情检查：

- 主键重复；
- OHLC 合法性；
- volume / amount 非法值；
- 缺失交易日；
- 停牌与无成交；
- 上市前数据；
- 退市后数据；
- 极端价格跳跃。

Universe 检查至少抽查：

```text
2015
2018
2020
2026
```

每年的历史上市证券数量应合理变化。

财务检查：

- report_period；
- announce_date；
- duplicate；
- revision。

期货检查：

- expire date；
- settlement；
- open interest；
- volume；
- 主力切换；
- 现货指数映射。

---

# Phase 9 — Storage Audit

全量初始化结束后，Codex 必须输出真实容量报告，而不是只报告预估值。

至少列出：

```text
raw_stock_daily_gb
processed_stock_daily_gb
financial_gb
reference_status_gb
index_futures_gb
derived_gb
duckdb_gb
logs_gb
project_total_gb
qmt_cache_gb（如果可获取）
```

同时列出 Top 20 最大文件 / 目录。

如果项目持久化数据超过 25GB，必须解释原因。

如果超过 30GB，必须提出压缩 / 去重 / 分区调整方案。

如果达到 40GB，不再自动扩展 dataset。

---

## 8. 为后续研究预留的统一接口

未来 Factor / Strategy / Backtest / Risk 都不直接读取 QMT。

统一读取：

```text
Processed / Derived Parquet
        ↑
      DuckDB
```

建议提供稳定的数据访问 API，例如：

```python
get_daily_bar(...)
get_universe(...)
get_financial_pit(...)
get_index_bar(...)
get_future_contracts(...)
get_future_main(...)
get_future_basis(...)
```

后续增加分钟、ETF、可转债、商品期货时，只增加新的 dataset / adapter，不修改策略和回测对底层数据的主要接口。

---

## 9. 后续扩展规则

新数据类型接入必须经过：

```text
Capability Probe
    ↓
Schema Definition
    ↓
Storage Estimate
    ↓
Small Sample Benchmark
    ↓
Capacity Check
    ↓
Raw
    ↓
Processed
    ↓
DuckDB View
```

禁止因“还有磁盘空间”直接下载全量新数据。

任何新增 dataset 在全量执行前必须回答：

1. 用于什么研究 / 策略？
2. 是否已经有替代数据？
3. 预计多少行？
4. Raw / Processed 各预计多少 GB？
5. 是否需要永久保存，还是可以重建？
6. 是否突破 30GB / 40GB 阈值？

---

## 10. Codex 最终交付物

至少提交：

```text
config/
    data_config.yaml

src/
    qmt_client.py
    storage_guard.py
    universe.py
    download_daily.py
    download_financial.py
    download_reference.py
    download_futures.py
    transform_market.py
    build_main_contract.py
    build_basis.py
    quality.py

scripts/
    preflight_database.py
    init_database.py
    update_daily.py
    validate_database.py
    storage_audit.py

tests/
    ...

docs/
    DATABASE_SCHEMA.md
    DATA_QUALITY_REPORT.md
    STORAGE_REPORT.md
```

并更新 `README.md`，说明：

- 如何初始化；
- 如何增量更新；
- 如何查询；
- 实际数据覆盖时间；
- 实际存储空间；
- 已知数据缺口；
- 如何增加新的 dataset。

---

## 11. v1 完成标准

只有满足以下条件才认为数据库 v1 完成：

- 历史 A 股证券全集可用，退市证券不丢失；
- A 股日线可查询且可增量更新；
- Raw / Processed 分层明确；
- 财务数据具有 Point-in-Time 语义；
- 复权数据可以重建；
- 主要指数可查询；
- IF / IH / IC / IM 实际合约历史可查询；
- 主力映射和基差可从真实合约重建；
- DuckDB 可以高效查询 Parquet；
- Factor / Strategy / Backtest / Risk 不需要直接访问 QMT；
- 全量流程可断点续传、重复执行；
- 数据质量报告完成；
- Storage Report 完成；
- 项目持久化数据原则上不超过 25GB；
- 超过 30GB 必须有明确原因；
- 基础库不允许超过 40GB 硬限制。

---

## 12. 当前最终范围冻结

```text
History:
2011-01-01 -> Current

A Shares:
SH + SZ + BJ
Level 1
1d only

Core:
Security Master
Historical Universe
Daily OHLCV / Amount / PreClose / Suspend
Trading Calendar
Corporate Actions / Adjustment
Financial 8 Tables / Point-in-Time

Index:
000001 / 000016 / 000300 / 000905 / 000852

Futures:
CFFEX IF / IH / IC / IM
Actual Historical Contracts
1d

Derived:
Adjusted Price
Historical Universe
Main Contract Mapping
Main Continuous Series
Index Futures Basis

Architecture:
QMT
 -> Raw Parquet
 -> Processed Parquet
 -> DuckDB
 -> Factor / Strategy / Backtest / Risk

Storage:
Persistent target <= 25GB
Warning >= 30GB
Hard limit >= 40GB
Total future project ceiling <= 100GB

Excluded from v1:
Options
Level 2
Full-market Minute
Full-market Tick
Trading API
Hong Kong / US / Overseas
Commodity Spot
```
